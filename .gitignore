# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
dist/

# Test & coverage artifacts
*.test
*.out
coverage.*
profile.*

# Go workspace files. go.work itself is committed: the ydb submodule depends on
# the root module, and without the workspace a fresh clone cannot build either
# until the root module is published.
go.work.sum

# Environment / secrets
.env
.env.*
!.env.example

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
